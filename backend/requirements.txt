fdb==2.0.1              # Driver Firebird oficial (compatível com Firebird 2.5)
pandas==2.2.2           # Última versão estável (suporte até Python 3.12)
matplotlib==3.8.4       # Estável e compatível com pandas 2.2
flask==3.0.3            # Última versão com suporte a Flask Blueprints, async, etc.
